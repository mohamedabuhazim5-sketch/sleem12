This folder contains the files explicitly provided in the uploaded consolidated codebase text.

Not included because they were not present as code blocks in the upload:
- app/layout.tsx
- app/globals.css
- any default files generated by create-next-app (package.json, tsconfig.json, next.config.*, etc.)

Use create-next-app first, then copy/replace these files into the generated project.
